spring:
  profiles:
    include: caas
  jackson:
    default-property-inclusion: NON_NULL


directory:
  datasource:
    url: jdbc:postgresql://dev4lab0.labs.mastercard.com:9981/bps?currentSchema=discovery&ApplicationName=bpsservice-directorydev
    username: bpsservice
    password: bps!2345
    driver-class-name: org.postgresql.Driver

keycloak:
  auth-server-url: http://dev4lab0.labs.mastercard.com:9990/bps-auth
  realm: test
  resource: bps
  public-client: "true"

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web: DEBUG
        0: DEBUG
      hibernate: ERROR

webhook:
  secretKey: v7aJHjbbxASKiwDW5wq6
org:
  slf4j:
    simpleLogger:
      defaultLogLevel: debug

adapter:
  health:
    endpoint: http://dev4lab0.labs.mastercard.com:9969/actuator/health
  oauth:
    admin:
      endpoint: http://dev4lab0.labs.mastercard.com:9970/api/v1/

swagger:
  url:
    rules: http://dev4lab0.labs.mastercard.com:9985/bps-rules/api/v0.1
    landing: http://dev.labs.mastercard.com/bps-discovery/api/v0.1/swagger-ui.html
    tokenAccess: http://dev4lab0.labs.mastercard.com:9990/bps-auth/realms/test/protocol/openid-connect/token
url:
  validator: ${swagger.url.rules}/validations
  authorization: ${swagger.url.rules}/pre-validations

