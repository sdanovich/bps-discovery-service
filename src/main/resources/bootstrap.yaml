spring:
  application:
    name: ${APP_NAME:bps-discovery}
  cloud:
      vault:
        enabled: ${VAULT_ENABLED:false}
        authentication: TOKEN
        token: ${VAULT_TOKEN:s.b7XAtDzxvBxnWGJweeolHS2A}
        uri: ${VAULT_ADDR:https://dev.labs.mastercard.com}
        scheme: http
        kv:
          application-name: application
          enabled: true
          backend: ${VAULT_BACKEND:bps-api}
          default-context:
        generic:
          enabled: false
        connection-timeout: 5000
        read-timeout: 15000
        config:
          order: -10
        ssl:
          trustStore: classpath:${VAULT_TRUSTSTORE}
          trustStorePassword: ${VAULT_PASSWORD}
